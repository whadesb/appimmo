<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vérification 2FA</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center">Vérification à deux facteurs (2FA)</h1>
        <p class="text-center">Veuillez entrer le code à 6 chiffres généré par votre application Google Authenticator.</p>

        <!-- Formulaire pour entrer le code 2FA -->
        <form action="/2fa" method="POST" class="mt-4">
            <div class="form-group">
                <label for="token">Code de vérification 2FA</label>
                <input type="text" class="form-control" id="token" name="token" placeholder="Entrez le code 2FA" required>
            </div>
            <button type="submit" class="btn btn-primary btn-block">Vérifier</button>
        </form>

        <% if (typeof error !== 'undefined') { %>
            <div class="alert alert-danger mt-4">
                <%= error %>
            </div>
        <% } %>

        <!-- Lien pour se déconnecter -->
        <div class="text-center mt-3">
            <a href="/logout">Se déconnecter</a>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
