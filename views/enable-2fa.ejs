<!DOCTYPE html>
<html lang="<%= locale %>">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="">
  <meta name="theme-color" content="#C4B990"> 
  <title>UAP Immo | <%= i18n.title %></title>
  <link href="/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/bootstrap-icons.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles-main.css">
  <style>
      body {
     font-family: 'Inter', sans-serif;
      background-color: #fff;
      color: #212529;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }
   main {
      flex: 1;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: calc(100vh - 80px);
      padding: 120px 20px 60px;
      background-color: #fff;
    }
    .twofa-container {
      max-width: 1100px;
      margin: auto;
      padding: 0 20px;
      display: flex;
      gap: 60px;
      flex-wrap: wrap;
      align-items: center;
      justify-content: space-between;
    }
    .twofa-left {
      flex: 1 1 48%;
    }
    .twofa-left h2 {
      font-size: 1.9rem;
      font-weight: 600;
      margin-bottom: 14px;
    }
    .twofa-left p {
      font-size: 1rem;
      color: #6c757d;
      margin-bottom: 20px;
    }
    .twofa-left ul {
      list-style: none;
      padding-left: 0;
      border-left: 3px solid #dee2e6;
      padding-left: 16px;
      margin-top: 12px;
    }
    .twofa-left li {
      font-size: 0.95rem;
      margin-bottom: 8px;
      color: #444;
    }
    .twofa-left li::before {
      content: "✓";
      color: #8f97c4;
      margin-right: 8px;
    }
    .twofa-form-wrapper {
      flex: 1 1 40%;
      background-color: #f8f9fa;
      padding: 32px;
      border-radius: 10px;
      border: 1px solid #e2e6ea;
    }
    .twofa-form-wrapper label {
      display: block;
      font-weight: 500;
      margin-bottom: 8px;
    }
    .btn-primary {
      width: 100%;
      background-color: #8f97c4;
      border: none;
      padding: 14px;
      font-size: 1rem;
      font-weight: 600;
      border-radius: 8px;
      transition: background-color 0.2s ease;
    }
    .btn-primary:hover {
      background-color: #7a81b8;
    }
    @media (max-width: 768px) {
      .twofa-container {
        flex-direction: column;
        gap: 40px;
      }
      .twofa-form-wrapper {
        width: 100%;
      }
      .twofa-left {
        text-align: center;
      }
      .twofa-left ul {
        margin: auto;
        max-width: 320px;
      }
    }
@media (min-width: 992px) {
  .twofa-container {
    min-height: 600px;
    align-items: center;
  }

  .twofa-left,
  .twofa-form-wrapper {
    display: flex;
    flex-direction: column;
    justify-content: center;
  }
}
   
  </style>
</head>
<body id="top">
  <main>
 <%- include('partials/navbar', { locale, isAuthenticated, currentPath }) %>
    <div class="twofa-container">
      <div class="twofa-left">
        <h2><%= i18n.instruction %></h2>
        <ul>
          <% i18n.steps.forEach(function(step) { %>
            <li><%= step %></li>
          <% }); %>
        </ul>
      </div>
      <div class="twofa-form-wrapper">
        <img src="<%= qrCode %>" alt="QR Code 2FA" class="img-fluid my-3 d-block mx-auto">
        <p class="text-center"><strong><%= i18n.secret_label %> :</strong> <code><%= user.twoFactorSecret %></code></p>
        <% if (messages.error && messages.error.length > 0) { %>
          <p class="text-danger text-center fw-bold"><%= messages.error[0] %></p>
        <% } %>
        <% if (messages.success && messages.success.length > 0) { %>
          <p class="text-success text-center fw-bold"><%= messages.success[0] %></p>
        <% } %>
        <form action="/<%= locale %>/enable-2fa" method="POST">
          <div class="mb-3">
            <label for="code" class="form-label"><%= i18n.codeLabel %></label>
            <input type="text" class="form-control text-center" name="code" id="code" required maxlength="6" pattern="\d{6}" placeholder="123456" readonly>
          </div>
<div id="keypad" class="mt-3 d-flex flex-wrap justify-content-center gap-2">
  <% for (let i = 1; i <= 9; i++) { %>
    <button type="button" class="btn btn-outline-secondary digit-button" data-digit="<%= i %>"><%= i %></button>
  <% } %>
  <button type="button" class="btn btn-outline-secondary digit-button" data-digit="0">0</button>
  <button type="button" class="btn btn-outline-danger" id="delete-digit">←</button>
</div>

          <button type="submit" class="btn btn-primary"> <%= i18n.verify %> </button>
        </form>
      </div>
    </div>
  </main>
<footer class="site-footer section-padding">
  <div class="container">
    <div class="row">
      <div class="col-lg-3 col-12 mb-4 pb-2">
        <a class="navbar-brand mb-2" href="/<%= locale %>">
          <span>UAP Immo</span>
        </a>
      </div>
      <div class="col-lg-3 col-md-4 col-6">
        <h6 class="site-footer-title mb-3">Informations</h6>
        <ul class="site-footer-links">
          <li class="site-footer-link-item"><a href="/<%= locale %>" class="site-footer-link">Accueil</a></li>
          <li class="site-footer-link-item"><a href="/<%= locale %>/register" class="site-footer-link">Créer un compte</a></li>
        </ul>
      </div>
      <div class="col-lg-3 col-md-4 col-6">
        <h6 class="site-footer-title mb-3">Accès</h6>
        <ul class="site-footer-links">
          <li class="site-footer-link-item"><a href="/<%= locale %>/contact" class="site-footer-link">Contact</a></li>
        </ul>
      </div>
      <div class="col-lg-3 col-md-4 col-12 mt-4 mt-lg-0 ms-auto">
        <div class="dropdown">
          <button class="btn btn-light border rounded-pill px-3 py-1 d-flex align-items-center gap-2" type="button" data-bs-toggle="dropdown" aria-expanded="false">
            <span class="lang-flag <%= locale === 'fr' ? 'lang-fr' : 'lang-en' %>"></span>
            <i class="bi bi-chevron-down small"></i>
          </button>
          <ul class="dropdown-menu dropdown-menu-end shadow">
            <% if (locale === 'fr') { %>
              <li><a class="dropdown-item d-flex align-items-center gap-2" href="/en<%= cleanPath %>"><span class="lang-flag lang-en"></span> <span>English</span></a></li>
            <% } else { %>
              <li><a class="dropdown-item d-flex align-items-center gap-2" href="/fr<%= cleanPath %>"><span class="lang-flag lang-fr"></span> <span>Français</span></a></li>
            <% } %>
          </ul>
        </div>
      </div>
    </div>
    <p class="copyright-text mt-lg-5 mt-4">Copyright © 2025 UAP Company. <%= i18n.footer && i18n.footer.all_rights ? i18n.footer.all_rights : 'Tous droits réservés.' %>
</p>
  </div>
</footer>


  <script src="/js/jquery.min.js"></script>
  <script src="/js/bootstrap.bundle.min.js"></script>
<script>
  document.addEventListener("DOMContentLoaded", function () {
    const input = document.getElementById("code");
    const digitButtons = document.querySelectorAll(".digit-button");
    const deleteBtn = document.getElementById("delete-digit");

    digitButtons.forEach(btn => {
      btn.addEventListener("click", () => {
        if (input.value.length < 6) {
          input.value += btn.dataset.digit;
        }
      });
    });

    deleteBtn.addEventListener("click", () => {
      input.value = input.value.slice(0, -1);
    });
  });
</script>

</body>
</html>
